# npm start 실행 방법 (Windows)

## 방법 1: 배치 파일 사용 (가장 간단) ⭐ 추천

### 단계
1. 프로젝트 폴더(`월드스키 카운터 계산기`)를 열기
2. `서버실행.bat` 파일을 **더블클릭**
3. 검은색 창(명령 프롬프트)이 열리면서 서버가 시작됩니다
4. "서버가 http://localhost:4000 에서 실행 중입니다" 메시지가 보이면 성공!

### 서버 중지 방법
- 검은색 창에서 `Ctrl + C` 키를 누르면 서버가 중지됩니다

---

## 방법 2: PowerShell 사용

### 단계 1: PowerShell 열기
1. 프로젝트 폴더(`월드스키 카운터 계산기`)를 열기
2. 폴더 내 빈 공간에서 **Shift + 마우스 우클릭**
3. 메뉴에서 **"PowerShell 창 여기에서 열기"** 또는 **"터미널에서 열기"** 선택

### 단계 2: npm start 실행
PowerShell 창이 열리면 다음 명령어를 입력하고 Enter 키를 누르세요:

```powershell
npm start
```

### 실행 결과 예시
```
> worldski-counter@1.0.0 start
> node server.js

월드스키 카운터 계산기 서버가 http://localhost:4000 에서 실행 중입니다.
또는 http://127.0.0.1:4000 로도 접속할 수 있습니다.
```

### 서버 중지 방법
- PowerShell 창에서 `Ctrl + C` 키를 누르면 서버가 중지됩니다

---

## 방법 3: 명령 프롬프트(CMD) 사용

### 단계 1: 명령 프롬프트 열기
1. **Windows 키 + R** 키를 누르기
2. "실행" 창이 열리면 `cmd` 입력 후 Enter
3. 또는 시작 메뉴에서 "명령 프롬프트" 검색 후 실행

### 단계 2: 프로젝트 폴더로 이동
명령 프롬프트 창에서 다음 명령어를 입력하고 Enter:

```cmd
cd "C:\Users\User\Desktop\월드스키 카운터 계산기"
```

> ⚠️ 주의: 위 경로는 예시입니다. 실제 프로젝트 폴더 경로로 변경하세요.
> 
> 프로젝트 폴더 경로 확인 방법:
> - 파일 탐색기에서 프로젝트 폴더를 열고
> - 주소창을 클릭하면 전체 경로가 표시됩니다
> - 그 경로를 복사해서 사용하세요

### 단계 3: npm start 실행
프로젝트 폴더로 이동한 후 다음 명령어를 입력하고 Enter:

```cmd
npm start
```

### 서버 중지 방법
- 명령 프롬프트 창에서 `Ctrl + C` 키를 누르면 서버가 중지됩니다

---

## 방법 4: Visual Studio Code 터미널 사용

### 단계
1. Visual Studio Code에서 프로젝트 폴더 열기
2. 상단 메뉴에서 **터미널 → 새 터미널** 선택 (또는 `Ctrl + `` 백틱 키)
3. 하단에 터미널 창이 열리면 다음 명령어 입력:

```bash
npm start
```

### 서버 중지 방법
- 터미널 창에서 `Ctrl + C` 키를 누르면 서버가 중지됩니다

---

## npm start 실행 전 확인 사항

### 1. Node.js 설치 확인
PowerShell이나 명령 프롬프트에서 다음 명령어로 확인:

```bash
node --version
npm --version
```

**결과 예시:**
```
v18.17.0
9.6.7
```

**설치되지 않은 경우:**
- https://nodejs.org 에서 Node.js LTS 버전 다운로드 및 설치
- 설치 후 컴퓨터 재시작

### 2. 의존성 패키지 설치 확인
프로젝트 폴더에 `node_modules` 폴더가 있는지 확인

**없는 경우 다음 명령어 실행:**
```bash
npm install
```

이 명령어는 `package.json`에 명시된 패키지들을 다운로드합니다.
처음 실행 시 몇 분 정도 걸릴 수 있습니다.

---

## 실행 중 발생할 수 있는 문제

### 문제 1: "npm은(는) 내부 또는 외부 명령...으로 인식되지 않습니다"
**원인:** Node.js가 설치되지 않았거나 PATH에 등록되지 않음

**해결:**
1. Node.js 재설치: https://nodejs.org
2. 설치 후 컴퓨터 재시작
3. 다시 시도

### 문제 2: "포트 4000이 이미 사용 중입니다"
**원인:** 다른 프로그램이 포트 4000을 사용 중

**해결:**
1. 다른 포트 사용:
   ```bash
   set PORT=4001
   npm start
   ```
   그 후 `http://localhost:4001` 접속

2. 또는 포트를 사용하는 프로그램 종료

### 문제 3: "Cannot find module 'express'"
**원인:** 의존성 패키지가 설치되지 않음

**해결:**
```bash
npm install
```

### 문제 4: 한글 경로 문제
**원인:** 프로젝트 폴더 경로에 한글이 있어서 문제 발생

**해결:**
- 가능하면 영문 경로로 프로젝트 폴더 이동
- 예: `C:\Projects\worldski-counter`

---

## 서버 실행 확인 방법

서버가 정상적으로 실행되면:

1. **터미널에 메시지 표시:**
   ```
   월드스키 카운터 계산기 서버가 http://localhost:4000 에서 실행 중입니다.
   ```

2. **브라우저에서 접속:**
   - 크롬, 엣지 등 브라우저 열기
   - 주소창에 `http://localhost:4000` 입력 후 Enter
   - 메인 페이지가 표시되면 성공!

3. **요금표 관리 페이지 접속:**
   - `http://localhost:4000/pricing.html` 접속
   - 공휴일 관리 기능 사용 가능

---

## 팁

- 서버를 실행한 터미널 창은 **닫지 마세요**. 서버가 계속 실행됩니다.
- 서버를 중지하려면 터미널 창에서 `Ctrl + C`를 누르세요.
- 개발 중에는 `npm run dev`를 사용하면 파일 변경 시 자동으로 서버가 재시작됩니다.

