# localhost:4000 접속 문제 해결 가이드

## 1. 서버가 실행 중인지 확인

### 확인 방법
- 터미널/명령 프롬프트 창이 열려있고 "서버가 http://localhost:4000 에서 실행 중입니다" 메시지가 보여야 합니다.
- 서버가 실행되지 않았다면 `서버실행.bat` 파일을 더블클릭하거나 터미널에서 `npm start` 실행

## 2. 포트가 이미 사용 중인지 확인

### 확인 방법
1. `포트확인.bat` 파일을 실행하거나
2. 터미널에서 다음 명령어 실행:
   ```bash
   netstat -ano | findstr :4000
   ```

### 해결 방법
- 포트가 사용 중이면 다른 포트 사용:
  ```bash
  set PORT=4001
  npm start
  ```
- 또는 해당 포트를 사용하는 프로그램 종료

## 3. 브라우저 주소 확인

### 올바른 주소
- ✅ `http://localhost:4000`
- ✅ `http://127.0.0.1:4000`

### 잘못된 주소
- ❌ `localhost:4000` (http:// 없음)
- ❌ `file:///C:/...` (파일 직접 열기)

## 4. 방화벽 확인

Windows 방화벽이 Node.js를 차단할 수 있습니다.

### 해결 방법
1. Windows 설정 → 개인 정보 보호 및 보안 → Windows 보안
2. 방화벽 및 네트워크 보호
3. 앱이 방화벽을 통과하도록 허용
4. Node.js가 차단 목록에 있으면 허용

## 5. Node.js 설치 확인

### 확인 방법
터미널에서:
```bash
node --version
npm --version
```

### 설치되지 않은 경우
- https://nodejs.org 에서 Node.js LTS 버전 다운로드 및 설치

## 6. 의존성 패키지 설치 확인

### 확인 방법
프로젝트 폴더에 `node_modules` 폴더가 있는지 확인

### 설치되지 않은 경우
```bash
npm install
```

## 7. 서버 에러 확인

서버 실행 시 터미널에 에러 메시지가 표시됩니다. 에러 내용을 확인하세요.

### 일반적인 에러
- **포트 사용 중**: 다른 포트 사용 또는 프로세스 종료
- **파일 없음**: `pricing.js` 또는 `holidays.json` 파일 확인
- **권한 문제**: 관리자 권한으로 실행

## 8. 브라우저 캐시 문제

### 해결 방법
- Ctrl + Shift + R (강력 새로고침)
- 또는 개발자 도구(F12) → 네트워크 탭 → "캐시 비활성화" 체크

## 9. 다른 포트로 실행

포트 4000이 문제인 경우:

```bash
set PORT=3000
npm start
```

그 후 `http://localhost:3000` 접속

## 10. 여전히 작동하지 않는 경우

1. 터미널의 전체 에러 메시지 확인
2. 브라우저 개발자 도구(F12) → 콘솔 탭에서 에러 확인
3. 서버를 재시작 (Ctrl+C 후 다시 `npm start`)

